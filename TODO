* Edit app's max accounts
* Edit app's mail templates
* Allow multiple origins
* Store create and update timestamps with every item
* Set appropriate cache headers on resources
* Provide mail templates when creating an app
  * or use a default?
* Let JWT expire after 15 minutes
  * Add Refresh mechanism
* Fix race condition in account creation
* Notification to me when a new app is created
